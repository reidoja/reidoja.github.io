import Typed from 'typed.js';

var options = {
	strings: [ 'I am Reinardus Ronaldo Raharja' ],
	loop: true,
	showCursor: false,
	typeSpeed: 100,
	backSpeed: 70
};

var typed = new Typed('.typing', options);
